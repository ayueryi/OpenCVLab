<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Responsive Spacing Values -->
    <Thickness x:Key="Spacing.XSmall">4</Thickness>
    <Thickness x:Key="Spacing.Small">8</Thickness>
    <Thickness x:Key="Spacing.Medium">16</Thickness>
    <Thickness x:Key="Spacing.Large">24</Thickness>
    <Thickness x:Key="Spacing.XLarge">32</Thickness>
    <Thickness x:Key="Spacing.XXLarge">48</Thickness>

    <!-- Responsive Font Sizes -->
    <System:Double xmlns:System="clr-namespace:System;assembly=mscorlib"
            x:Key="FontSize.XSmall">11</System:Double>
    <System:Double xmlns:System="clr-namespace:System;assembly=mscorlib"
            x:Key="FontSize.Small">12</System:Double>
    <System:Double xmlns:System="clr-namespace:System;assembly=mscorlib"
            x:Key="FontSize.Regular">14</System:Double>
    <System:Double xmlns:System="clr-namespace:System;assembly=mscorlib"
            x:Key="FontSize.Medium">16</System:Double>
    <System:Double xmlns:System="clr-namespace:System;assembly=mscorlib"
            x:Key="FontSize.Large">20</System:Double>
    <System:Double xmlns:System="clr-namespace:System;assembly=mscorlib"
            x:Key="FontSize.XLarge">24</System:Double>
    <System:Double xmlns:System="clr-namespace:System;assembly=mscorlib"
            x:Key="FontSize.Title">28</System:Double>
    <System:Double xmlns:System="clr-namespace:System;assembly=mscorlib"
            x:Key="FontSize.Display">36</System:Double>

    <!-- Responsive Corner Radius -->
    <CornerRadius x:Key="CornerRadius.Small">6</CornerRadius>
    <CornerRadius x:Key="CornerRadius.Medium">12</CornerRadius>
    <CornerRadius x:Key="CornerRadius.Large">16</CornerRadius>
    <CornerRadius x:Key="CornerRadius.XLarge">20</CornerRadius>
    <CornerRadius x:Key="CornerRadius.Round">999</CornerRadius>

    <!-- Responsive Card Container -->
    <Style x:Key="ResponsiveCard"
            TargetType="Border">
        <Setter Property="Background"
                Value="{StaticResource OpenCVLab.Brush.Surface}"/>
        <Setter Property="CornerRadius"
                Value="{StaticResource CornerRadius.XLarge}"/>
        <Setter Property="Padding"
                Value="{StaticResource Spacing.Large}"/>
        <Setter Property="Effect"
                Value="{StaticResource EffectShadow2}"/>
        <Setter Property="Margin"
                Value="{StaticResource Spacing.Medium}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver"
                    Value="True">
                <Setter Property="Effect"
                        Value="{StaticResource EffectShadow3}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Section Header Style -->
    <Style x:Key="SectionHeader"
            TargetType="TextBlock">
        <Setter Property="FontSize"
                Value="{StaticResource FontSize.Large}"/>
        <Setter Property="FontWeight"
                Value="SemiBold"/>
        <Setter Property="Foreground"
                Value="{StaticResource OpenCVLab.Brush.Text}"/>
        <Setter Property="Margin"
                Value="0,0,0,16"/>
    </Style>

    <!-- Section Subheader Style -->
    <Style x:Key="SectionSubheader"
            TargetType="TextBlock">
        <Setter Property="FontSize"
                Value="{StaticResource FontSize.Medium}"/>
        <Setter Property="FontWeight"
                Value="Medium"/>
        <Setter Property="Foreground"
                Value="{StaticResource OpenCVLab.Brush.Text}"/>
        <Setter Property="Margin"
                Value="0,0,0,12"/>
    </Style>

    <!-- Body Text Style -->
    <Style x:Key="BodyText"
            TargetType="TextBlock">
        <Setter Property="FontSize"
                Value="{StaticResource FontSize.Regular}"/>
        <Setter Property="Foreground"
                Value="{StaticResource OpenCVLab.Brush.Text}"/>
        <Setter Property="LineHeight"
                Value="22"/>
        <Setter Property="TextWrapping"
                Value="Wrap"/>
    </Style>

    <!-- Caption Text Style -->
    <Style x:Key="CaptionText"
            TargetType="TextBlock">
        <Setter Property="FontSize"
                Value="{StaticResource FontSize.Small}"/>
        <Setter Property="Foreground"
                Value="{StaticResource OpenCVLab.Brush.TextMuted}"/>
        <Setter Property="LineHeight"
                Value="18"/>
    </Style>

    <!-- Responsive Grid Panel -->
    <Style x:Key="ResponsiveGrid"
            TargetType="Grid">
        <Setter Property="Margin"
                Value="{StaticResource Spacing.Medium}"/>
    </Style>

    <!-- Responsive Stack Panel -->
    <Style x:Key="ResponsiveStackPanel"
            TargetType="StackPanel">
        <Setter Property="Margin"
                Value="{StaticResource Spacing.Medium}"/>
    </Style>

    <!-- Icon Button Style -->
    <Style x:Key="IconButton"
            TargetType="Button"
            BasedOn="{StaticResource OpenCVLabButtonBase}">
        <Setter Property="Width"
                Value="40"/>
        <Setter Property="Height"
                Value="40"/>
        <Setter Property="Padding"
                Value="0"/>
        <Setter Property="BorderThickness"
                Value="0"/>
        <Setter Property="Background"
                Value="Transparent"/>
    </Style>

    <!-- Floating Action Button -->
    <Style x:Key="FloatingActionButton"
            TargetType="Button">
        <Setter Property="Width"
                Value="56"/>
        <Setter Property="Height"
                Value="56"/>
        <Setter Property="Background"
                Value="{StaticResource OpenCVLab.Brush.Gradient.Primary}"/>
        <Setter Property="Foreground"
                Value="White"/>
        <Setter Property="BorderThickness"
                Value="0"/>
        <Setter Property="Cursor"
                Value="Hand"/>
        <Setter Property="Effect"
                Value="{StaticResource EffectShadow3}"/>
        <Setter Property="RenderTransformOrigin"
                Value="0.5,0.5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Ellipse
                            x:Name="PART_Background"
                            Fill="{TemplateBinding Background}"/>
                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver"
                                Value="True">
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect
                                        BlurRadius="32"
                                        Opacity="0.35"
                                        ShadowDepth="6"
                                        Color="#0984E3"/>
                                </Setter.Value>
                            </Setter>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                            To="1.08"
                                            Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                            To="1.08"
                                            Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                            To="1"
                                            Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                            To="1"
                                            Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed"
                                Value="True">
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.96"
                                            ScaleY="0.96"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1"
                        ScaleY="1"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Divider Style -->
    <Style x:Key="Divider"
            TargetType="Border">
        <Setter Property="Height"
                Value="1"/>
        <Setter Property="Background"
                Value="{StaticResource OpenCVLab.Brush.Border}"/>
        <Setter Property="Margin"
                Value="0,16"/>
        <Setter Property="HorizontalAlignment"
                Value="Stretch"/>
    </Style>

    <!-- Badge Style -->
    <Style x:Key="Badge"
            TargetType="Border">
        <Setter Property="Background"
                Value="{StaticResource OpenCVLab.Brush.Accent}"/>
        <Setter Property="CornerRadius"
                Value="{StaticResource CornerRadius.Round}"/>
        <Setter Property="Padding"
                Value="8,4"/>
        <Setter Property="HorizontalAlignment"
                Value="Left"/>
        <Setter Property="VerticalAlignment"
                Value="Center"/>
    </Style>

    <!-- Chip Style -->
    <Style x:Key="Chip"
            TargetType="Border">
        <Setter Property="Background"
                Value="{StaticResource OpenCVLab.Brush.Background}"/>
        <Setter Property="BorderBrush"
                Value="{StaticResource OpenCVLab.Brush.Border}"/>
        <Setter Property="BorderThickness"
                Value="1"/>
        <Setter Property="CornerRadius"
                Value="{StaticResource CornerRadius.Round}"/>
        <Setter Property="Padding"
                Value="12,6"/>
        <Setter Property="HorizontalAlignment"
                Value="Left"/>
        <Setter Property="VerticalAlignment"
                Value="Center"/>
    </Style>

    <!-- Tooltip Style -->
    <Style x:Key="ModernTooltip"
            TargetType="ToolTip">
        <Setter Property="Background"
                Value="{StaticResource OpenCVLab.Brush.Text}"/>
        <Setter Property="Foreground"
                Value="White"/>
        <Setter Property="BorderThickness"
                Value="0"/>
        <Setter Property="Padding"
                Value="12,8"/>
        <Setter Property="FontSize"
                Value="{StaticResource FontSize.Small}"/>
        <Setter Property="Effect"
                Value="{StaticResource EffectShadow3}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border
                        Background="{TemplateBinding Background}"
                        CornerRadius="8"
                        Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
