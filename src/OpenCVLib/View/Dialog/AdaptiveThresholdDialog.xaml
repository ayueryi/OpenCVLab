<UserControl
    x:Class="OpenCVLab.View.Dialog.AdaptiveThresholdDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:OpenCVLab.View.Dialog"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://Yu-UI.com//2024/xaml"
    d:DataContext="{d:DesignInstance local:AdaptiveThresholdDialog, IsDesignTimeCreatable=False}"
    d:DesignHeight="240"
    d:DesignWidth="560"
    mc:Ignorable="d">

    <Border Effect="{StaticResource EffectShadow3}"
            Style="{StaticResource OpenCVLabDialogRootBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ui:Title MainTitle="自适应阈值参数"
                    SubTitle="Adaptive Threshold"/>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <StackPanel Margin="20"
                        Orientation="Vertical">
                    <StackPanel Margin="0,4"
                            Orientation="Horizontal">
                        <TextBlock Width="110"
                                VerticalAlignment="Center">BlockSize</TextBlock>
                        <ui:NumericBox Width="200"
                                Value="{Binding BlockSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Margin="0,4"
                            Orientation="Horizontal">
                        <TextBlock Width="110"
                                VerticalAlignment="Center">阈值类型</TextBlock>
                        <ComboBox Width="200"
                                SelectedIndex="{Binding SelectedThresholdTypes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <ComboBoxItem Content="Binary"/>
                            <ComboBoxItem Content="BinaryInv"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="0,4"
                            Orientation="Horizontal">
                        <TextBlock Width="110"
                                VerticalAlignment="Center">MaxValue</TextBlock>
                        <ui:NumericBox Width="200"
                                Value="{Binding ThresholdMaxValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Row="1"
                        Style="{StaticResource OpenCVLabDialogFooterBorder}">
                    <UniformGrid Columns="2">
                        <Button Click="Confirm"
                            Style="{StaticResource OpenCVLabDialogPrimaryButton}">
                            <StackPanel Orientation="Horizontal">
                                <ui:Icon Kind="CheckCircleOutline"
                                                        Margin="0,0,8,0" />
                                <TextBlock VerticalAlignment="Center">确认</TextBlock>
                            </StackPanel>
                        </Button>

                        <Button Click="Cancel"
                            Style="{StaticResource OpenCVLabDialogSecondaryButton}">
                            <StackPanel Orientation="Horizontal">
                                <ui:Icon Kind="CloseCircleOutline"
                                                        Margin="0,0,8,0" />
                                <TextBlock VerticalAlignment="Center">取消</TextBlock>
                            </StackPanel>
                        </Button>
                    </UniformGrid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
