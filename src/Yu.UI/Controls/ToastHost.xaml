<UserControl
    x:Class="Yu.UI.Controls.ToastHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Yu.UI.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="ToastCard"
                TargetType="Border">
            <Setter Property="CornerRadius"
                    Value="10"/>
            <Setter Property="Padding"
                    Value="12"/>
            <Setter Property="Margin"
                    Value="0,0,0,10"/>
            <Setter Property="Background"
                    Value="White"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="BorderBrush"
                    Value="#14000000"/>
            <Setter Property="Opacity"
                    Value="0"/>
            <Setter Property="RenderTransformOrigin"
                    Value="1,1"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform X="0"
                            Y="10"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="14"
                            ShadowDepth="0"
                            Opacity="0.25"
                            Color="#FF000000"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                    To="1"
                                    Duration="0:0:0.18">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                                    To="0"
                                    Duration="0:0:0.18">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <local:ToastSeverityToBrushConverter x:Key="SeverityBrush"/>
        <local:BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </UserControl.Resources>

    <Grid IsHitTestVisible="True">
        <ItemsControl ItemsSource="{Binding Toasts, RelativeSource={RelativeSource AncestorType=local:ToastHost}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource ToastCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="6"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Background="{Binding Severity, Converter={StaticResource SeverityBrush}}"
                                    CornerRadius="10,0,0,10"/>

                            <TextBlock Grid.Column="1"
                                    Margin="10,0,0,0"
                                    TextWrapping="Wrap"
                                    Foreground="#FF222222"
                                    FontSize="13"
                                    Text="{Binding Message}"/>

                            <Button
                                Grid.Column="2"
                                Margin="10,0,0,0"
                                Width="26"
                                Height="26"
                                Padding="0"
                                IsTabStop="False"
                                Focusable="False"
                                Click="CloseToast_Click"
                                Background="Transparent"
                                BorderThickness="0"
                                Visibility="{Binding ShowCloseButton, Converter={StaticResource BoolToVisibility}}"
                                IsHitTestVisible="True">
                                <TextBlock Text="×"
                                        FontSize="16"
                                        Foreground="#99000000"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
